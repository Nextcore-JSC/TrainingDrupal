<?php

/**
 * Implements hook_suggestions_alter() .
 */
function lioa_suggestions_alter(array &$suggestions, array $variables, $hook) {
kint($variables);
  // Add an interface-language specific suggestion to all theme hooks.
  // $suggestions[] = $hook . '__' . \Drupal::languageManager()
  //   ->getCurrentLanguage()
  //   ->getId();
}